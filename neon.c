#include<stdio.h>
int main()
{
    int a;
    printf("Enter number :");
    scanf("%d",&a);
    // int sum=0;
    int sqare;
    // int rem;
    while(a>0)
    {
        sqare=a*a;
        // rem=sqare%10;
        // sum=sum+sqare;
        // a++;
    }
    printf("%d",sqare);
    return 0;
}